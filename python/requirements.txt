# For local image generation with GPU
diffusers>=0.22.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0
pillow>=9.0.0
scipy>=1.9.0

# Firebase/Google Cloud
firebase-admin>=6.0.0
google-cloud-storage>=2.10.0
google-cloud-firestore>=2.11.0

# HTTP requests for API fallback
requests>=2.31.0

# PyTorch with CUDA support (for GPU)
# Install from: https://pytorch.org/get-started/locally/
# Example: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Or use CPU version if no GPU:
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch torchvision torchaudio
